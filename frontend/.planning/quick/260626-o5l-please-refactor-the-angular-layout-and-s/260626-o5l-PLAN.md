---
phase: quick/260626-o5l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/features/dashboard/menu/menu-detail/menu-detail.component.html
  - src/app/features/dashboard/menu/menu-detail/menu-detail.component.scss
autonomous: true
requirements: []
must_haves:
  truths:
    - "The table header title 'Piatti del Menu' and subtitle are fully left-aligned"
    - "The format selector toggle and 'Stampa Selezionati' button are grouped together, side-by-side on the right side of the table header"
    - "The 'Stampa Selezionati' button is no longer present in the hero section"
    - "The 'Stampa Selezionati' button retains disabled behavior when selection is empty"
    - "The format selector block uses theme-matching background and border (not custom #faf6f0/#eae1d8)"
    - "The PDF download button remains in the hero section unchanged"
  artifacts:
    - path: "src/app/features/dashboard/menu/menu-detail/menu-detail.component.html"
      contains: "table-format-selector"
    - path: "src/app/features/dashboard/menu/menu-detail/menu-detail.component.scss"
      contains: "table-format-selector"
  key_links:
    - from: "menu-detail.component.html"
      to: "menu-detail.component.scss"
      via: "class bindings"
      pattern: "class=\"table-format-selector\""
---

<objective>
Refactor the menu detail view's table card header layout and format selector styling — move the batch print button from the hero section into a right-aligned action group alongside the format toggle, and theme-integrate the selector block.

Purpose: Consolidate all table-level actions into the table card header (title left, actions right) and align styling with the site's neutral color palette.
Output: Updated `menu-detail.component.html` (template restructure) + `menu-detail.component.scss` (theme-aligned styles).
</objective>

<execution_context>
@/Users/gabrielesuardi/Desktop/fe-roscoff/.opencode/get-shit-done/workflows/execute-quick.md
</execution_context>

<context>
@src/app/features/dashboard/menu/menu-detail/menu-detail.component.html
@src/app/features/dashboard/menu/menu-detail/menu-detail.component.scss
@src/styles.css

<interfaces>
From menu-detail.component.ts:
- `selection` is `SelectionModel<any>` from `@angular/cdk/collections` — exposes `.isEmpty()`, `.selected`, `.clear()`, `.toggle(item)`, `.isSelected(item)`
- `printBatch()` method — called on button click, internally checks `selection.selected.length` and shows error if empty
- `selectedPrintFormat` is a `signal<'standard' | 'cortilia' | 'foorban'>` — bound to toggle group via `[(value)]` and `(valueChange)`
- `downloadMenuPdf()` — remains in hero section, no behavioral change

Theme colors from styles.css `:root`:
  - `--gold-primary: #d4a20a`, `--gold-primary-hover: #b88909`
  - Table header row bg: `#fafafa` (styles.css line 113)
  - Dropdown/menu border: `#e0e0e0` (styles.css line 141, 181)
  - Card border: `#f0f0f0`
  - Hover bg: `#f5f5f5`
  - White surfaces: `#ffffff`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure table card header — move batch print button into right-aligned action group</name>
  <files>
    src/app/features/dashboard/menu/menu-detail/menu-detail.component.html
  </files>
  <action>
    Make two targeted changes to the template:

    **Change A — Remove "Stampa Selezionati" from hero section (lines 45-54):**
    Delete the entire "Stampa Selezionati" button block (lines 45-54 inclusive) from within `.hero-cta-container`. Keep the PDF download button (lines 36-43) and the surrounding `.hero-actions-side-simple` / `.hero-cta-container` structure intact.

    **Change B — Add "Stampa Selezionati" into the table header action group (inside `.table-format-selector`):**
    After the closing `</mat-button-toggle-group>` tag (currently line 79), insert the moved button. The `.table-format-selector` div becomes a flex container holding two children: the existing format group (label + toggle) on the left within the container, and the "Stampa Selezionati" button on the right. Add a spacer element between them to push them apart.

    The resulting `.table-format-selector` structure should be:
    ```
    <div class="table-format-selector">
      <div class="format-toggle-section">
        <p class="format-label-inline">Formato di Stampa:</p>
        <mat-button-toggle-group ...>...</mat-button-toggle-group>
      </div>
      <span class="action-group-spacer"></span>
      <button mat-flat-button color="accent" class="hero-download-btn batch-print-header-btn"
        (click)="printBatch()" [disabled]="selection.isEmpty()"
        matTooltip="Stampa le etichette ZPL per i piatti selezionati">
        <mat-icon>print</mat-icon>
        Stampa Selezionati
      </button>
    </div>
    ```

    Key details per D-01 (layout re-alignment):
    - Title text group ("Piatti del Menu" + subtitle) stays as the first child of `table-card-header-flex` — left-aligned via `justify-content: space-between` on the parent
    - The `.table-format-selector` div is the second child of `table-card-header-flex` — right-aligned naturally by the flex parent's `space-between`
    - The button must use `[disabled]="selection.isEmpty()"` to retain its semantic behavior per D-02

    No comments in the updated template. Preserve all other structural elements (mat-card, hero section, PDF button, table, category blocks, empty state) exactly as-is.
  </action>
  <verify>
    <automated>
      grep -c 'Stampa Selezionati' src/app/features/dashboard/menu/menu-detail/menu-detail.component.html | xargs test 2 -eq && echo "OK: button appears twice (toggle + menu)" || echo "FAIL: expected button to appear exactly 2 times (once in format selector, once elsewhere if applicable)"
    </automated>
  </verify>
  <done>
    - Template no longer has "Stampa Selezionati" button inside `.hero-cta-container`
    - Template has "Stampa Selezionati" button inside `.table-format-selector`, after the toggle group
    - Button has `[disabled]="selection.isEmpty()"` preserving its original behavior
    - PDF button remains in hero section
    - Title + subtitle are the first child of `table-card-header-flex` (left-aligned)
    - `.table-format-selector` is the second child (right-aligned)
    - No HTML comments present
  </done>
</task>

<task type="auto">
  <name>Task 2: Theme-integrate format selector styles and add batch button layout</name>
  <files>
    src/app/features/dashboard/menu/menu-detail/menu-detail.component.scss
  </files>
  <action>
    Apply three style changes to the SCSS file:

    **Change A — Replace `.table-format-selector` custom colors with theme variables (lines 104-112):**
    ```
    .table-format-selector {
      display: flex;
      align-items: center;
      gap: 12px;
      background-color: #fafafa;
      padding: 6px 12px;
      border-radius: 8px;
      border: 1px solid #e0e0e0;
    }
    ```
    - Per D-03 (theme integration): `background-color: #fafafa` replaces `#faf6f0` — matches the table header row background used globally (styles.css line 113)
    - `border: 1px solid #e0e0e0` replaces `#eae1d8` — matches the neutral dropdown/menu border used globally (styles.css lines 141, 181)

    **Change B — Add `.format-toggle-section` wrapper style:**
    Add a new rule to wrap the label + toggle group together so they stay as a unit:
    ```
    .format-toggle-section {
      display: flex;
      align-items: center;
      gap: 12px;
    }
    ```

    **Change C — Add `.action-group-spacer` and `.batch-print-header-btn` styles:**
    ```
    .action-group-spacer {
      flex: 1 1 auto;
    }

    .batch-print-header-btn {
      padding: 0 20px;
      height: 36px !important;
      font-size: 0.85rem !important;
      border-radius: 18px;
      white-space: nowrap;
      line-height: 36px;
    }
    ```
    - `flex: 1 1 auto` on the spacer pushes the button to the right within `.table-format-selector`
    - The button style uses a slightly more compact height (36px vs the hero's 40px) and smaller font to better fit in the table header line, but keeps the pill-shaped `border-radius: 18px` consistent with `.hero-download-btn` pattern

    No comments. Remove no other existing styles (`.hero-cta-container`, `.hero-actions-side-simple`, etc. remain since they still serve the PDF button).
  </action>
  <verify>
    <automated>
      grep -c '#faf6f0\|#eae1d8' src/app/features/dashboard/menu/menu-detail/menu-detail.component.scss | xargs test 0 -eq && echo "OK: custom colors removed" || echo "FAIL: custom colors still present"
    </automated>
  </verify>
  <done>
    - `.table-format-selector` uses `background-color: #fafafa` and `border: 1px solid #e0e0e0` (no traces of `#faf6f0` or `#eae1d8`)
    - `.format-toggle-section` exists to group label + toggle
    - `.action-group-spacer` exists to push button right
    - `.batch-print-header-btn` exists with compact pill shape for table header context
    - No comments in file
  </done>
</task>

</tasks>

<verification>
1. `grep -c '#faf6f0\|#eae1d8' src/app/features/dashboard/menu/menu-detail/menu-detail.component.scss` returns 0
2. `grep -c 'Stampa Selezionati' src/app/features/dashboard/menu/menu-detail/menu-detail.component.html` returns exactly 1 (only in table header, not in hero section)
3. `grep 'selection.isEmpty()' src/app/features/dashboard/menu/menu-detail/menu-detail.component.html` confirms the disabled binding on the moved button
4. `grep 'Scarica PDF' src/app/features/dashboard/menu/menu-detail/menu-detail.component.html` confirms PDF button remains in hero section
</verification>

<success_criteria>
- The "Piatti del Menu" header is fully left-aligned, format selector + batch button are grouped on the right
- "Stampa Selezionati" button appears only in the table card header (not in the hero section)
- Button is disabled when no rows are selected
- Format selector has a clean neutral background (#fafafa) and border (#e0e0e0), matching the rest of the dashboard's table header styling
- PDF download button remains in the hero section unaffected
- No HTML or SCSS comments in the updated files
- `ng build` or `ng serve` compiles without errors
</success_criteria>

<output>
After completion, the files modified directly serve as the deliverable — no SUMMARY.md needed for quick tasks.
</output>
