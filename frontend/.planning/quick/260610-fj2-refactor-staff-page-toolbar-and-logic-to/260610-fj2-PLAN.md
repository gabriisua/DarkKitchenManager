---
phase: quick
plan: 260610-fj2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/features/dashboard/staff/staff-page.component.html
  - src/app/features/dashboard/staff/staff-page.component.ts
autonomous: true
must_haves:
  truths:
    - "Toolbar uses `.toolbar-row` layout (not `.header-actions`)"
    - "Search field uses `.toolbar-field.search-field` classes"
    - "Reset button appears when search has value, disabled when empty"
    - "Spacer div separates reset button from CTA"
    - "Nuovo Utente CTA uses `mat-raised-button` with `toolbar-cta` class (no inline style)"
    - "Component exposes `hasActiveFilters()` and `clearFilters()` methods matching category/allergen pattern"
    - "No functional regression — search, sort, pagination, CRUD all still work"
  artifacts:
    - path: "src/app/features/dashboard/staff/staff-page.component.html"
      contains: "toolbar-row"
    - path: "src/app/features/dashboard/staff/staff-page.component.ts"
      contains: "hasActiveFilters"
    - path: "src/app/features/dashboard/staff/staff-page.component.ts"
      contains: "clearFilters"
  key_links:
    - from: "staff-page.component.html"
      to: "staff-page.component.ts"
      via: "hasActiveFilters() in [disabled] binding"
      pattern: "hasActiveFilters"
    - from: "staff-page.component.html"
      to: "staff-page.component.ts"
      via: "clearFilters() in (click) binding"
      pattern: "clearFilters"
---

<objective>
Refactor staff-page toolbar HTML and component TS to match the established category-page/allergen-page pattern.

**Purpose:** Consistency across all entity pages (category, allergen, staff) — same toolbar layout, same filter logic, same CTA button styling.

**Output:**
- `staff-page.component.html` — updated toolbar with `.toolbar-row`, Reset button with `hasActiveFilters()`, spacer, styled CTA
- `staff-page.component.ts` — added `hasActiveFilters()` and `clearFilters()` methods
</objective>

<execution_context>
@/Users/gabrielesuardi/Desktop/fe-roscoff/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/quick/260610-fj2-refactor-staff-page-toolbar-and-logic-to/260610-fj2-PLAN.md

## Reference Pattern (category component)

**HTML toolbar structure (category.component.html lines 15-46):**
- `<div gridSearch class="toolbar-row">` wraps all toolbar controls
- `<mat-form-field>` uses `class="toolbar-field search-field"`
- Reset button: `mat-stroked-button`, `(click)="clearFilters()"`, `[disabled]="!hasActiveFilters()"`, `mat-icon` with `clear_all`
- `<div class="spacer"></div>` between reset and CTA
- CTA: `mat-raised-button color="primary" class="toolbar-cta"` — no inline margin

**TS filter helpers (category.component.ts lines 104-119):**
- `hasActiveFilters(): boolean { return !!this.searchTerm || this.isActiveFilter !== null; }`
- `clearFilters(): void { this.searchTerm = ''; ... reset query with search: '' ... }`

## Global Styles

`.toolbar-row`, `.spacer`, `.toolbar-cta`, `.toolbar-field` are all defined in `src/styles.css`. No component-level CSS changes needed.

## Current Staff State

**HTML** (`staff-page.component.html`):
- Uses `class="header-actions"` instead of `class="toolbar-row"`
- Search field uses `class="search-field"` (missing `toolbar-field`)
- No Reset button, no spacer
- CTA has `style="margin-left: 10px"` inline instead of `class="toolbar-cta"`

**TS** (`staff-page.component.ts`):
- Has `searchTerm: string = ''`, `onSearch(value: string)`, `updateQuery(partial)`
- Missing: `hasActiveFilters()`, `clearFilters()`
</context>

<tasks>

<task type="auto">
  <name>Refactor staff-page toolbar to match category/allergen pattern</name>
  <files>
    src/app/features/dashboard/staff/staff-page.component.html
    src/app/features/dashboard/staff/staff-page.component.ts
  </files>
  <action>
    **Part 1 — HTML (`staff-page.component.html`):**

    1. Change `<div gridSearch class="header-actions">` to `<div gridSearch class="toolbar-row">`
    2. Change the `<mat-form-field>` from `class="search-field"` to `class="toolbar-field search-field"`
    3. Remove `style="margin-left: 10px"` from the CTA button, add `class="toolbar-cta"` instead (keep `mat-raised-button color="primary"`)
    4. Insert a Reset button between the search field and the CTA (matching category/allergen pattern):
       ```html
       <button
         mat-stroked-button
         (click)="clearFilters()"
         matTooltip="Resetta i filtri"
         [disabled]="!hasActiveFilters()">
         <mat-icon>clear_all</mat-icon>
         Reset
       </button>
       ```
    5. Insert `<div class="spacer"></div>` between the Reset button and the CTA button

    **Part 2 — TS (`staff-page.component.ts`):**

    Add two methods matching the established pattern (staff only has a `searchTerm` filter, no `isActiveFilter`):

    ```ts
    hasActiveFilters(): boolean {
      return !!this.searchTerm;
    }

    clearFilters(): void {
      this.searchTerm = '';
      this.updateQuery({ search: '', page: 1 });
    }
    ```

    Place these after the existing `onSearch()` method (around line 110), keeping methods grouped logically.

    **What NOT to change:**
    - Do NOT change the `onSearch(value: string)` signature — the HTML still uses `(ngModelChange)="onSearch($event)"`
    - Do NOT alter `updateQuery()`, `onSortChange()`, `onPageChange()`, `refreshTable()` or any CRUD methods
    - Do NOT change imports or the component decorator
    - Do NOT touch the CSS file (uses global styles)
    - Do NOT change the `<ng-template #actionsTemplate>` block
  </action>
  <verify>
    <automated>
      # Verify HTML has all required structural elements
      grep -c 'toolbar-row' src/app/features/dashboard/staff/staff-page.component.html || echo "FAIL: missing toolbar-row"
      grep -c 'toolbar-field search-field' src/app/features/dashboard/staff/staff-page.component.html || echo "FAIL: missing toolbar-field class"
      grep -c 'hasActiveFilters' src/app/features/dashboard/staff/staff-page.component.html || echo "FAIL: missing hasActiveFilters binding"
      grep -c 'clearFilters' src/app/features/dashboard/staff/staff-page.component.html || echo "FAIL: missing clearFilters binding"
      grep -c 'class="spacer"' src/app/features/dashboard/staff/staff-page.component.html || echo "FAIL: missing spacer"
      grep -c 'toolbar-cta' src/app/features/dashboard/staff/staff-page.component.html || echo "FAIL: missing toolbar-cta class"
      grep -c 'header-actions' src/app/features/dashboard/staff/staff-page.component.html && echo "FAIL: header-actions still present" || echo "OK: header-actions removed"
      grep -c 'margin-left' src/app/features/dashboard/staff/staff-page.component.html && echo "FAIL: inline margin still present" || echo "OK: inline margin removed"

      # Verify TS has the required methods
      grep -c 'hasActiveFilters' src/app/features/dashboard/staff/staff-page.component.ts || echo "FAIL: missing hasActiveFilters method"
      grep -c 'clearFilters' src/app/features/dashboard/staff/staff-page.component.ts || echo "FAIL: missing clearFilters method"
      echo "--- All checks complete ---"
    </automated>
  </verify>
  <done>
    - Toolbar uses `.toolbar-row` layout with Reset button, spacer, and CTA
    - Search field uses `.toolbar-field.search-field` classes
    - Reset button disabled when no active search, enabled when search has value
    - CTA is `mat-raised-button color="primary"` with `toolbar-cta` class, no inline style
    - `hasActiveFilters()` and `clearFilters()` methods exist in component TS
    - All grep verification checks pass
    - Application compiles without errors
  </done>
</task>

</tasks>

<verification>
Run: `ng build --configuration production` or `ng build` to verify compilation.
Run the grep checks from the verify block above to confirm all structural changes.
</verification>

<success_criteria>
- Staff page toolbar visually matches category/allergen toolbar layout
- Reset button appears, respects disabled/enabled state based on search value
- Clicking Reset clears search and reloads table
- Nuovo Utente button is `mat-raised-button` with `toolbar-cta` class
- Zero inline styles (`style="margin-left"` removed)
- Build compiles successfully
</success_criteria>

<output>
After completion, create `.planning/quick/260610-fj2-refactor-staff-page-toolbar-and-logic-to/260610-fj2-SUMMARY.md`
</output>
