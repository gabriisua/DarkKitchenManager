---
quick_id: 260616-gh9
slug: scan-all-angular-components-in-the-proje
date: 2026-06-16
status: planned
mode: quick
tasks: 6
---

# Plan: Angular Component Refactoring — Global Design System Alignment

## Task 1: Replace local SCSS variables with CSS custom properties

**Files:**
- src/app/layout/navbar/navbar.component.scss
- src/app/layout/sidebar/sidebar.component.scss
- src/app/features/dashboard/plate/plate-detail/plate-detail.component.scss
- src/app/features/dashboard/plate/plate-form/plate-form.component.scss
- src/app/features/dashboard/menu/menu-form/menu-form.component.scss
- src/app/layout/layout.component.scss

**Action:** Delete all `$variable:` declarations and replace usages with `var(--...)`. Map:
- `$gold-primary` → `var(--gold-primary)`
- `$gold-hover` → `var(--gold-primary-hover)`
- `$gold-light-bg` → `var(--gold-primary-light)`
- `$border-color` → remove (use inline border-color or keep if non-standard)
- `$neutral-dark` / `$text-dark` / `$text-primary` → remove (use hardcoded or var)
- `$text-light` / `$text-muted` → remove
- `$sidebar-bg`, `$sidebar-hover`, `$sidebar-active-bg` → keep as layout-specific (not brand)
- `$radius-standard` → keep or replace
- `$bg-color` → keep or replace
- `$sidebar-width` → keep (layout constant)

**Verify:** No `$gold-`, `$border-color`, `$neutral-dark`, `$text-dark`, `$text-light`, `$bg-color` variables remain in component files.

## Task 2: Replace hardcoded brand colors with CSS variables

**Files:**
- src/app/layout/navbar/navbar.component.scss
- src/app/layout/sidebar/sidebar.component.scss
- src/app/shared/ui.overlay/ui-overlay.component.css
- src/app/features/dashboard/plate/plate-detail/plate-detail.component.scss
- src/app/features/dashboard/plate/plate-form/plate-form.component.scss
- src/app/features/dashboard/allergen/allergen-dialog/allergen-dialog.component.css
- src/app/features/dashboard/sale/sale-plate-form/sale-plate-form.component.css
- src/app/features/dashboard/sale/sale-category-form/sale-category-form.component.css
- src/app/features/auth/login/login.component.scss

**Action:** Replace hardcoded values:
- `#d4a20a` → `var(--gold-primary)`
- `#b88909` → `var(--gold-primary-hover)`
- `#fff8e1` → `var(--gold-primary-light)`
- `rgba(212, 162, 10, 0.15)` → `var(--gold-primary-lightest)`
- `rgba(212, 162, 10, 0.25)` → add to styles.css or use `var(--gold-primary-lightest)`
- `rgba(212,162,10,0.2)` → use `var(--gold-primary-lightest)` or add as `--gold-primary-lightest`
- `rgba(212,162,10,0.1)` → use `var(--gold-primary-lightest)` or keep as is
- `rgba(214,162,10,0.3)` → use `var(--gold-primary-lightest)` or add new var
- `#c8a45d`, `#b88a3a` (login gradient) → use brand gold variables

## Task 3: Remove local Material component overrides

**Files:**
- src/app/features/dashboard/allergen/allergen-dialog/allergen-dialog.component.css
- src/app/features/dashboard/sale/sale-plate-form/sale-plate-form.component.css
- src/app/features/dashboard/sale/sale-category-form/sale-category-form.component.css
- src/app/features/dashboard/plate/plate-form/plate-form.component.scss
- src/app/features/dashboard/plate/plate-detail/plate-detail.component.scss
- src/app/shared/data-grid/data-grid.component.css
- src/app/layout/navbar/navbar.component.scss
- src/app/layout/sidebar/sidebar.component.scss
- src/app/features/dashboard/customer/hub-dialog/hub-dialog.component.css
- src/app/features/dashboard/customer/customer-form/customer-form.component.css
- src/app/features/dashboard/ingredient/ingredient-dialog/ingredient-dialog.component.css

**Action:** Delete CSS blocks targeting:
- `mat-select-panel`, `mat-option`
- `mat-autocomplete-panel`
- `mat-datepicker*`, `mat-calendar*` (centralized in styles.css)
- `mat-card*` (centralized in styles.css)
- `mat-dialog*`, `mdc-dialog*`
- `mat-table`, `mat-mdc-header-cell`, `mat-mdc-cell`, `mdc-data-table*`
- `mat-form-field`, `mat-mdc-form-field*`, `mdc-text-field*`
- `mat-divider`
- `mat-expansion-panel*`
- `mat-mdc-menu-panel`, `mat-mdc-menu-item`
- `cdk-overlay-container`
- `::ng-deep` overrides of Material components

**Note:** Keep component-specific layout styles (padding, margin, flex) that are not Material overrides. Remove ONLY the Material-specific selectors.

## Task 4: Remove/replace inline styles in HTML templates

**Files:**
- src/app/features/dashboard/invoice/invoice-page.component.html
- src/app/features/dashboard/plate/plate-detail/plate-detail.component.html
- src/app/features/dashboard/plate/plate-form/plate-form.component.html
- src/app/shared/ui.overlay/ui-overlay.component.html
- src/app/features/dashboard/menu/menu-form/menu-form.component.html
- src/app/features/dashboard/customer/customer-form/customer-form.component.html
- src/app/features/dashboard/category/category.component.html
- src/app/features/dashboard/order/order-details/order-details.component.html
- All page heading files with `style="margin: 0"`

**Action:**
- `style="margin: 0"` on page headings → remove (handled by global or component styles)
- `width: 100%` → use `full-width` class or component style
- `text-align: *` → use `text-center` class or component style
- `display: flex; justify-content: space-between;` → use `page-header` class or component style
- Move remaining critical inline styles to the component's stylesheet
- `style="font-weight: bold"` → use `<strong>` or CSS class
- `style="color: ..."` → use CSS variables in component stylesheet

## Task 5: Apply global utility classes to HTML templates

**Files:** All HTML templates in src/app/

**Action:**
- Add `.page-container` and `.page-header` to standard page layouts
- Add `.accent-title` to creative/distinctive headings using brush font
- Add `.gold-btn` to primary CTA buttons (currently use `mat-raised-button color="primary"`)
- Add `.full-width` where `width: 100%` is needed
- Add `.text-center` for centered text
- Add `.mt-16`, `.mb-16` for consistent spacing

## Task 6: Update global styles.css with missing CSS variables

**Files:**
- src/styles.css

**Action:** Add any CSS variables needed by components that can't use the existing ones:
- `--gold-primary-lightest` already exists as `rgba(212, 162, 10, 0.15)`
- If other opacity variants are needed (0.1, 0.2, 0.25, 0.3), consider adding them or use `rgba()` directly with the variable -- note CSS var in rgba requires modern browser support, so use hardcoded rgba for now or use @supports
