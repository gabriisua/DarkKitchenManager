---
quick_id: 260610-f84
type: ui-refactor
subsystem: ui
tags: [allergen, toolbar, search, filter, pagination]
duration: ~3min
completed: 2026-06-10
key-files:
  modified:
    - src/app/features/dashboard/allergen/allergen-page.component.ts
  verified-unchanged:
    - src/app/features/dashboard/allergen/allergen-page.component.html
---

# Quick Task 260610-f84: Hard-replace allergen toolbar + fix clearFilters() pageSize reset

**Hard-rewrote toolbar (confirmed compliant) and fixed clearFilters() to explicitly reset pageSize to 10**

## Task Completion

| # | Task | Status | Commit |
|---|------|--------|--------|
| 1 | Hard-replace allergen toolbar + fix clearFilters() pageSize reset | ✅ Done | `98bdc89` |

## Files Modified

- `src/app/features/dashboard/allergen/allergen-page.component.ts` — `clearFilters()` now calls `updateQuery({ search: '', page: 1, pageSize: 10 })` instead of omitting `pageSize`, ensuring pagination is fully reset on filter clear

## Files Verified (no changes needed)

- `src/app/features/dashboard/allergen/allergen-page.component.html` — Toolbar block already matched spec exactly (hard rewrite confirmed identically compliant)

## Verification Results

| Check | Expected | Actual | Result |
|-------|----------|--------|--------|
| `pageSize: 10` in clearFilters() | line in clearFilters | line 104 | ✅ |
| `*ngIf="filters.search"` count | 1 | 1 | ✅ |
| `Nuovo Allergene` count | 1 | 1 | ✅ |
| `class="spacer"` count | 1 | 1 | ✅ |
| `toolbar-cta` count | 1 | 1 | ✅ |

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check

- Commits verified: `98bdc89` exists in git log
- File changes verified: only 1 changed file (1 insertion, 1 deletion in .ts)
- All 5 automated verification checks pass
