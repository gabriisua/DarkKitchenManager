---
quick_id: 260610-f84
type: ui-refactor
phase: quick
plan: 1
wave: 1
depends_on: []
files_modified:
  - src/app/features/dashboard/allergen/allergen-page.component.html
  - src/app/features/dashboard/allergen/allergen-page.component.ts
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Allergen page toolbar contains: search field, clear button (shown only when filters.search is truthy), spacer, and 'Nuovo Allergene' CTA button"
    - "Clear button uses mat-icon-button with clear icon and appears conditionally via *ngIf=\"filters.search\""
    - "Clicking clear button or pressing Enter in search field resets search, page=1, and pageSize=10"
  artifacts:
    - path: "src/app/features/dashboard/allergen/allergen-page.component.html"
      provides: "Hard-replaced toolbar with search, clear, spacer, Nuovo Allergene"
    - path: "src/app/features/dashboard/allergen/allergen-page.component.ts"
      provides: "clearFilters() that resets search, page=1, pageSize=10"
  key_links:
    - from: "allergen-page.component.html"
      to: "allergen-page.component.ts"
      via: "clearFilters() called by clear button (click) handler"
    - from: "allergen-page.component.ts :: clearFilters()"
      to: "allergen-page.component.ts :: updateQuery()"
      via: "calls updateQuery with { search: '', page: 1, pageSize: 10 }"
---

<objective>
Hard-replace the allergen page toolbar with the specified structure (search field, conditional clear button, spacer, "Nuovo Allergene" CTA) and update `clearFilters()` to explicitly reset the query with page=1, pageSize=10.

Purpose: Ensure the allergen toolbar matches the exact specification — no drift from previous refactoring. The clear button must only appear when a search filter is active, and clearing must reset pagination to page 1 with pageSize 10.
Output: Updated allergen-page.component.html and allergen-page.component.ts.
</objective>

<execution_context>
@/Users/gabrielesuardi/Desktop/fe-roscoff/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
## Current State

**allergen-page.component.html toolbar (lines 12-35):**
```
<div gridSearch class="toolbar-row">
  <mat-form-field appearance="outline" class="toolbar-field">
    <mat-label>Cerca allergeni...</mat-label>
    <input matInput type="text" [(ngModel)]="filters.search" (keyup.enter)="onSearch()">
    <mat-icon matSuffix>search</mat-icon>
  </mat-form-field>

  <button *ngIf="filters.search" mat-icon-button color="warn"
          (click)="clearFilters()" matTooltip="Rimuovi filtro">
    <mat-icon>clear</mat-icon>
  </button>

  <div class="spacer"></div>

  <button mat-raised-button color="primary" class="toolbar-cta" (click)="openCreateDialog()">
    <mat-icon>add</mat-icon>
    Nuovo Allergene
  </button>
</div>
```

The toolbar HTML is structurally similar to what's specified, but must be hard-replaced to guarantee exact compliance.

**allergen-page.component.ts clearFilters() (lines 102-105):**
```typescript
clearFilters(): void {
    this.filters.search = '';
    this.updateQuery({ search: '', page: 1 });
}
```

The current implementation does NOT pass `pageSize: 10`. Since `updateQuery` only sets `pageSize` when the partial includes `pageSize`, the current call leaves the previous pageSize unchanged. Requirement demands explicit `pageSize: 10` on clear.

**AllergenPagedRequest extends PagedRequest** which defines:
```typescript
interface PagedRequest {
  page: number;         // ✓
  pageSize: number;     // ✓ available
  search?: string;
  sortColumn?: string;
  sortDirection?: 'asc' | 'desc' | '';
}
```
`pageSize` is a valid field on the request — passing it in the partial is type-safe.

## Files to Modify

1. **allergen-page.component.html** — Hard-replace `<div gridSearch class="toolbar-row">...</div>` with exact specified structure
2. **allergen-page.component.ts** — Update `clearFilters()` to pass `{ search: '', page: 1, pageSize: 10 }`
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Hard-replace allergen toolbar + fix clearFilters() pageSize reset</name>
  <files>
    src/app/features/dashboard/allergen/allergen-page.component.html
    src/app/features/dashboard/allergen/allergen-page.component.ts
  </files>
  <action>
    **allergen-page.component.html — Hard-replace the toolbar:**

    Replace the entire `<div gridSearch class="toolbar-row">` block (lines 12-35) with the exact structure below.
    The replacement must be a hard rewrite — delete lines 12 through 35 and insert the new block:

    ```html
        <div gridSearch class="toolbar-row">

          <mat-form-field appearance="outline" class="toolbar-field">
            <mat-label>Cerca allergeni...</mat-label>
            <input matInput type="text" [(ngModel)]="filters.search" (keyup.enter)="onSearch()">
            <mat-icon matSuffix>search</mat-icon>
          </mat-form-field>

          <button
            *ngIf="filters.search"
            mat-icon-button
            color="warn"
            (click)="clearFilters()"
            matTooltip="Rimuovi filtro">
            <mat-icon>clear</mat-icon>
          </button>

          <div class="spacer"></div>

          <button mat-raised-button color="primary" class="toolbar-cta" (click)="openCreateDialog()">
            <mat-icon>add</mat-icon>
            Nuovo Allergene
          </button>
        </div>
    ```

    Verify the indentation matches — the `<div gridSearch>` should be at the same indentation level as the surrounding `<app-data-grid>` bindings (12 spaces / 4 levels). Each child of `gridSearch` gets one additional indent level beyond the div.

    Do NOT change: the page-container div, app-data-grid bindings, actionsTemplate, or any other part of the file.

    **allergen-page.component.ts — Update clearFilters():**

    Replace the `clearFilters()` method (currently lines 102-105) with:

    ```typescript
      clearFilters(): void {
        this.filters.search = '';
        this.updateQuery({ search: '', page: 1, pageSize: 10 });
      }
    ```

    The key change: pass `pageSize: 10` explicitly. This ensures that after clearing the search, the query reverts to exactly page 1 with 10 items per page, regardless of any prior pageSize change from pagination events.

    Do NOT change: any other methods, imports, decorators, or class members.
  </action>
  <verify>
    <automated>grep -n 'pageSize: 10' src/app/features/dashboard/allergen/allergen-page.component.ts && grep -n 'gridSearch' src/app/features/dashboard/allergen/allergen-page.component.html | grep 'toolbar-row'</automated>
  </verify>
  <done>
    - allergen-page.component.html toolbar contains exactly: search mat-form-field, clear mat-icon-button with *ngIf="filters.search", spacer div, Nuovo Allergene mat-raised-button
    - allergen-page.component.ts clearFilters() calls updateQuery with { search: '', page: 1, pageSize: 10 }
    - No other changes to either file
  </done>
</task>

</tasks>

<verification>
1. `grep -n 'pageSize: 10' src/app/features/dashboard/allergen/allergen-page.component.ts` — must return line with clearFilters()
2. `grep -c '\*ngIf="filters.search"' src/app/features/dashboard/allergen/allergen-page.component.html` — must return 1
3. `grep -c 'Nuovo Allergene' src/app/features/dashboard/allergen/allergen-page.component.html` — must return 1
4. `grep -c 'class="spacer"' src/app/features/dashboard/allergen/allergen-page.component.html` — must return 1
5. `grep -c 'toolbar-cta' src/app/features/dashboard/allergen/allergen-page.component.html` — must return 1
</verification>

<success_criteria>
- Toolbar contains exactly: search field (mat-form-field) → clear button (mat-icon-button, conditional) → spacer → "Nuovo Allergene" CTA (mat-raised-button)
- `clearFilters()` resets filters.search to '', then calls `updateQuery({ search: '', page: 1, pageSize: 10 })`
- Clear button is only visible when `filters.search` is truthy (non-empty string)
- No unused `[disabled]`, no extra buttons, no layout drift from spec
</success_criteria>

<output>
After completion, this plan is self-contained. No SUMMARY file needed for a quick task.
</output>
